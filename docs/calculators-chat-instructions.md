
# Инструкции за чата - Калкулатори (/calculators)

## ВАЖНО: Файлова структура
- **НЕ РЕДАКТИРАЙ** `src/app/calculators/page.tsx` (само SEO metadata)
- **РЕДАКТИРАЙ САМО** `src/app/calculators/CalculatorsClient.tsx` (всичката логика)

## Основни команди за чата:

### 1. ПРОМЯНА НА ЦЕНИ
```
"Промени цената на SEO Struktor от 1980 на 2200 лв"
"Обнови цените на всички системи: SEO-1980, Trendlab-3450, Clickstarter-1570, Clientomat-2890"
```

### 2. ДОБАВЯНЕ НА НОВИ ПОЛЕТА
```
"Добави ново поле 'Размер на екипа' в SEO Struktor калкулатора"
"Създай dropdown за 'Тип бизнес' с опции: B2B, B2C, B2B2C"
```

### 3. ПРОМЯНА НА ИЗЧИСЛЕНИЯ
```
"Промени ROI формулата за Clickstarter да използва 3x вместо 1.85x"
"Обнови timeframe на Trendlab от '2-4 месеца' на '1-3 месеца'"
```

### 4. ДОБАВЯНЕ НА ПРЕПОРЪКИ
```
"Добави нова препоръка за SEO Struktor: 'Локално SEO' когато индустрията е 'Ресторантьорство'"
"Създай условие за Clientomat: ако AOV > 5000 → препоръчай 'VIP програма'"
```

### 5. UI/UX ПОДОБРЕНИЯ
```
"Направи калкулатора по-mobile friendly"
"Добави loading анимация при смяна на табовете"
"Промени цвета на бутоните от жълт на син"
```

### 6. ДОБАВЯНЕ НА НОВА СИСТЕМА
```
"Създай нов калкулатор 'EmailEngine™' с полета: subscribers, open rate, CTR"
"Добави пети таб за 'SocialBoost™' система"
```

### 7. АНИМАЦИИ И ЕФЕКТИ
```
"Добави hover ефект на Score кръга"
"Направи результатите да се появяват с fade-in анимация"
"Добави пулсиращ ефект на активния таб"
```

### 8. ВАЛИДАЦИЯ И ГРЕШКИ
```
"Добави валидация - всички полета задължителни преди показване на резултати"
"Покажи грешка ако потребителят въведе отрицателни числа"
"Добави tooltip с обяснение на всяко поле"
```

## Структура на CalculatorsClient.tsx:

### Ключови секции за редактиране:

**1. Services Array (ред ~670):**
```typescript
const services = [
  {
    id: "seo-struktor",
    name: "SEO Struktor™",
    monthlyPrice: 1980, // ← ПРОМЯНА НА ЦЕНИ ТУК
    // ...
  }
]
```

**2. Input Configuration (ред ~85):**
```typescript
const getServiceInputs = () => {
  switch (serviceName) {
    case "SEO Struktor™":
      return {
        param1: { // ← ДОБАВЯНЕ НА НОВИ ПОЛЕТА ТУК
          key: "currentTraffic",
          label: "Текущ месечен трафик",
          // ...
        }
      }
  }
}
```

**3. Calculation Logic (ред ~170):**
```typescript
const calculateResults = () => {
  switch (serviceName) {
    case "SEO Struktor™": {
      const newTraffic = param1 * 3.4; // ← ПРОМЯНА НА ФОРМУЛИ ТУК
      // ...
    }
  }
}
```

**4. Recommendations (ред ~260):**
```typescript
switch (serviceName) {
  case "SEO Struktor™":
    if (param1 < 5000) recs.push("Органичен трафик"); // ← НОВИ ПРЕПОРЪКИ ТУК
    // ...
}
```

## Полезни промени за да поискаш:

### Функционални подобрения:
- "Добави бутон 'Сравни всички системи'"
- "Създай PDF export на резултатите"
- "Добави email форма за изпращане на резултатите"
- "Направи калкулатора да запомня данните в localStorage"

### Аналитика и tracking:
- "Добави Google Analytics tracking на действията"
- "Проследи кой калкулатор се използва най-често"
- "Добави Klaviyo event при попълване на калкулатор"

### A/B тестове:
- "Създай втора версия с различни цветове"
- "Тествай различни CTA текстове"
- "Пробвай различни позициониране на полетата"

## Важни забележки:
1. **Винаги тествай** промените в browser преди финализиране
2. **Запази оригиналните имена** на системите (с ™)
3. **Не променяй** основната структура на tabs
4. **Внимавай** с TypeScript типовете при добавяне на нови полета
5. **Тествай responsive** дизайна на мобилни устройства

## Бързи команди:
- "Покажи ми структурата на services array"
- "Обясни как работи calculateResults функцията"
- "Къде се намира логиката за препоръките"
- "Как да добавя нова анимация"
