'use client';

// Calculators are temporarily disabled
// Will be re-enabled when user requests

// Placeholder exports to prevent import errors
export const LazyROICalculator = () => null;
export const LazySEOCalculator = () => null;
export const LazyConversionCalculator = () => null;